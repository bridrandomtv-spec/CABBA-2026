rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // For this fan community applet, allow public read/write to posts
      // Note: In a real production app with users, we would restrict this.
      allow read, write: if true;
    }
  }
}
